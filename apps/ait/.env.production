# 이 파일은 production 빌드(`vite build` = `ait build`)에서만 로드된다.
# dev(`granite dev`/`vite`)에서는 로드되지 않으므로, 개발/샌드박스는 테스트 ID로 동작한다.
# (live 광고 ID를 dev/샌드박스에서 사용하면 AppsInToss 광고 정책 위반)
#
# AppsInToss(AIT) 마켓 전용 전면(Interstitial) 광고 adGroupId (live).
VITE_AIT_INTERSTITIAL_AD_ID=ait.v2.live.f2389330c1ce431a
