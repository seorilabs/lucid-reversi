// Copy to granite.config.ts after confirming AppsInToss appName and framework version.
// Keep registration-sensitive values explicit.

export default {
  appName: "확정 필요",
  brand: {
    displayName: "확정 필요",
    icon: "확정 필요"
  },
  web: {
    port: 5173,
    buildCommand: "npm run build",
    outputDirectory: "dist"
  }
};

