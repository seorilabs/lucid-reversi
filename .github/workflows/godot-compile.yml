name: Godot Compile

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: godot-compile-${{ github.ref }}
  cancel-in-progress: true

jobs:
  compile:
    name: Godot Compile
    runs-on: ${{ github.event.repository.private && 'seorilabs-rpi-arm64' || 'ubuntu-latest' }}
    timeout-minutes: 20
    env:
      GODOT_VERSION: "4.6.3"
      GODOT_STATUS: stable

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Godot Runtime Dependencies
        shell: bash
        run: |
          set -euo pipefail

          if ldconfig -p 2>/dev/null | grep -q "libfontconfig.so.1"; then
            echo "fontconfig runtime is already available"
            exit 0
          fi

          if ! command -v apt-get >/dev/null 2>&1; then
            echo "libfontconfig.so.1 is missing and apt-get is unavailable" >&2
            exit 1
          fi

          sudo_cmd=""
          if command -v sudo >/dev/null 2>&1; then
            sudo_cmd="sudo"
          fi

          ${sudo_cmd} apt-get update
          ${sudo_cmd} apt-get install -y libfontconfig1 fontconfig

      - name: Ensure Godot
        shell: bash
        run: bash scripts/ensure_godot.sh

      - name: Compile Project
        shell: bash
        run: bash scripts/godot_quality_gate.sh --project godot --smoke-scene res://tests/test_runner.tscn
